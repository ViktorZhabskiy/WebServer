# Install Golang
https://golang.org/doc/install

# Build project
1. Go to the project folder
2. Install dependencies
    ```golang
    go get
    ```
3. build project
    ```golang
    go build
    ``` 